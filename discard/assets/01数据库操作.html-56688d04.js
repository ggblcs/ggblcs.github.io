import{_ as e,p as a,q as d,a1 as s}from"./framework-96b046e1.js";const i={},l=s(`<h1 id="数据库操作" tabindex="-1"><a class="header-anchor" href="#数据库操作" aria-hidden="true">#</a> 数据库操作</h1><p>学习数据库之前先来认识几个单词</p><ol><li><code>local</code>本地</li><li><code>host</code>主机 ，简写为<code>-h</code></li><li><code>username</code>用户名，简写为<code>-u</code></li><li><code>password</code>密码，简写为<code>-p</code></li><li><code>port</code>端口号，简写为<code>-P</code></li></ol><h2 id="_01查看所有数据库" tabindex="-1"><a class="header-anchor" href="#_01查看所有数据库" aria-hidden="true">#</a> 01查看所有数据库</h2><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>show databases;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="_02创建数据库" tabindex="-1"><a class="header-anchor" href="#_02创建数据库" aria-hidden="true">#</a> 02创建数据库</h2><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>-- 创建数据库
create database [if not exists] \`数据库名\` [charset = 字符码];
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><ol><li><code>if not exists</code>是如果不存在的意思，上面的意思是如果表格××不存在，就创建。加上此句即使创建之前已经有这个数据库，也不会报错，可以省略。</li><li>数据库名加反引号\`\`可以防止数据库名是关键词而报错，省略后不能再用关键字作为数据库名，可以省略。</li><li><code>charset = 字符码</code>中常用字符码有<code>UTF8</code>、<code>gbk</code>、 <code>gb2312</code>。省略后默认字符集为初始默认设置，可以省略。</li></ol><p>创建数据库的简单写法：<code>create database 数据库名;</code></p><p>最后，来个例子吧：创建数据库<code>Myfirst</code>。</p><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>-- 复杂写法
create database if not exists Myfirst charset = utf8;
-- 简单写法
create database Myfirst;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_03删除数据库" tabindex="-1"><a class="header-anchor" href="#_03删除数据库" aria-hidden="true">#</a> 03删除数据库</h2><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>drop database [if exists] \`数据库名\`;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>同样的，只要替换数据库名就可以了：删除 <code>Myfirst</code>数据库。</p><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>-- 复杂写法
drop database if exists \`Myfirst\`;
-- 简单写法
drop database Myfirst;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_04显示数据库的sql语句" tabindex="-1"><a class="header-anchor" href="#_04显示数据库的sql语句" aria-hidden="true">#</a> 04显示数据库的sql语句</h2><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>show create database 数据库名;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>万变不离其宗，这个就不在举例子了。</p><h2 id="_05修改数据库" tabindex="-1"><a class="header-anchor" href="#_05修改数据库" aria-hidden="true">#</a> 05修改数据库</h2><p>首先说明一下，修改数据库只能修改数据库字符集，其他都不能修改，所以这条<code>sql</code>语句不怎么常用。</p><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>alter database \`数据库名\` charset = utf8;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>在这里，数据库名是可以更换的，<code>utf8</code>也是可以更换的内容。</p><p>数据库名是你所要修改的哪个数据库，<code>charset</code>等号后面的内容代表无论以前是什么字符集，这个数据库的字符集以后要修改为什么字符集，比如上面代表修改为<code>utf8</code>。</p><h2 id="_06选择数据库" tabindex="-1"><a class="header-anchor" href="#_06选择数据库" aria-hidden="true">#</a> 06选择数据库</h2><p>如果你想要在某个文件夹内放文件，最起码你要选择一个文件夹并且打开它；同样的，你必须选择一个数据库，才能在这个数据库中创建数据表。</p><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>use \`数据库名\`；
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>代码很短，但很重要。</p>`,27),n=[l];function c(r,o){return a(),d("div",null,n)}const u=e(i,[["render",c],["__file","01数据库操作.html.vue"]]);export{u as default};
